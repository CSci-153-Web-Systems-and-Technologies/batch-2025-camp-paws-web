-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.animal_groups (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name character varying NOT NULL,
  description text,
  animal_type_id character varying NOT NULL,
  sex_id character varying,
  color_pattern_id character varying,
  primary_color_id character varying,
  report_count integer NOT NULL DEFAULT 0,
  first_sighted_date date,
  last_sighted_date date,
  created_by uuid NOT NULL,
  created_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT animal_groups_pkey PRIMARY KEY (id),
  CONSTRAINT animal_groups_animal_type_id_fkey FOREIGN KEY (animal_type_id) REFERENCES public.animal_types(id),
  CONSTRAINT animal_groups_sex_id_fkey FOREIGN KEY (sex_id) REFERENCES public.sexes(id),
  CONSTRAINT animal_groups_color_pattern_id_fkey FOREIGN KEY (color_pattern_id) REFERENCES public.color_patterns(id),
  CONSTRAINT animal_groups_primary_color_id_fkey FOREIGN KEY (primary_color_id) REFERENCES public.primary_colors(id),
  CONSTRAINT animal_groups_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id)
);
CREATE TABLE public.animal_types (
  id character varying NOT NULL,
  name character varying NOT NULL UNIQUE,
  CONSTRAINT animal_types_pkey PRIMARY KEY (id)
);
CREATE TABLE public.cat_body_condition_scores (
  score integer NOT NULL,
  label character varying NOT NULL,
  description text,
  image_path text,
  CONSTRAINT cat_body_condition_scores_pkey PRIMARY KEY (score)
);
CREATE TABLE public.collar_statuses (
  id character varying NOT NULL,
  name character varying NOT NULL UNIQUE,
  CONSTRAINT collar_statuses_pkey PRIMARY KEY (id)
);
CREATE TABLE public.color_patterns (
  id character varying NOT NULL,
  name character varying NOT NULL,
  animal_type_id character varying,
  CONSTRAINT color_patterns_pkey PRIMARY KEY (id),
  CONSTRAINT color_patterns_animal_type_id_fkey FOREIGN KEY (animal_type_id) REFERENCES public.animal_types(id)
);
CREATE TABLE public.dog_body_condition_scores (
  score integer NOT NULL,
  label character varying NOT NULL,
  description text,
  image_path text,
  CONSTRAINT dog_body_condition_scores_pkey PRIMARY KEY (score)
);
CREATE TABLE public.eye_condition_types (
  id character varying NOT NULL,
  label character varying NOT NULL UNIQUE,
  description text,
  CONSTRAINT eye_condition_types_pkey PRIMARY KEY (id)
);
CREATE TABLE public.gait_condition_types (
  id character varying NOT NULL,
  label character varying NOT NULL UNIQUE,
  description text,
  CONSTRAINT gait_condition_types_pkey PRIMARY KEY (id)
);
CREATE TABLE public.group_reports (
  group_id uuid NOT NULL,
  report_id uuid NOT NULL,
  added_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  added_by uuid,
  CONSTRAINT group_reports_pkey PRIMARY KEY (group_id, report_id),
  CONSTRAINT group_reports_group_id_fkey FOREIGN KEY (group_id) REFERENCES public.animal_groups(id),
  CONSTRAINT group_reports_report_id_fkey FOREIGN KEY (report_id) REFERENCES public.stray_animal_reports(id),
  CONSTRAINT group_reports_added_by_fkey FOREIGN KEY (added_by) REFERENCES public.users(id)
);
CREATE TABLE public.primary_colors (
  id character varying NOT NULL,
  label character varying NOT NULL UNIQUE,
  color_class character varying,
  CONSTRAINT primary_colors_pkey PRIMARY KEY (id)
);
CREATE TABLE public.report_eye_conditions (
  report_id uuid NOT NULL,
  condition_id character varying NOT NULL,
  CONSTRAINT report_eye_conditions_pkey PRIMARY KEY (report_id, condition_id),
  CONSTRAINT report_eye_conditions_report_id_fkey FOREIGN KEY (report_id) REFERENCES public.stray_animal_reports(id),
  CONSTRAINT report_eye_conditions_condition_id_fkey FOREIGN KEY (condition_id) REFERENCES public.eye_condition_types(id)
);
CREATE TABLE public.report_gait_conditions (
  report_id uuid NOT NULL,
  condition_id character varying NOT NULL,
  CONSTRAINT report_gait_conditions_pkey PRIMARY KEY (report_id, condition_id),
  CONSTRAINT report_gait_conditions_report_id_fkey FOREIGN KEY (report_id) REFERENCES public.stray_animal_reports(id),
  CONSTRAINT report_gait_conditions_condition_id_fkey FOREIGN KEY (condition_id) REFERENCES public.gait_condition_types(id)
);
CREATE TABLE public.report_skin_conditions (
  report_id uuid NOT NULL,
  condition_id character varying NOT NULL,
  CONSTRAINT report_skin_conditions_pkey PRIMARY KEY (report_id, condition_id),
  CONSTRAINT report_skin_conditions_report_id_fkey FOREIGN KEY (report_id) REFERENCES public.stray_animal_reports(id),
  CONSTRAINT report_skin_conditions_condition_id_fkey FOREIGN KEY (condition_id) REFERENCES public.skin_condition_types(id)
);
CREATE TABLE public.sexes (
  id character varying NOT NULL,
  name character varying NOT NULL UNIQUE,
  CONSTRAINT sexes_pkey PRIMARY KEY (id)
);
CREATE TABLE public.skin_condition_types (
  id character varying NOT NULL,
  label character varying NOT NULL UNIQUE,
  description text,
  CONSTRAINT skin_condition_types_pkey PRIMARY KEY (id)
);
CREATE TABLE public.stray_animal_reports (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  photo_url text,
  animal_type_id character varying NOT NULL,
  sex_id character varying NOT NULL,
  collar_status_id character varying NOT NULL,
  color_pattern_id character varying,
  primary_color_id character varying,
  body_condition_score integer,
  additional_notes text,
  spotted_date date NOT NULL,
  spotted_time time with time zone,
  latitude numeric NOT NULL,
  longitude numeric NOT NULL,
  location_description text,
  status character varying NOT NULL DEFAULT 'pending'::character varying CHECK (status::text = ANY (ARRAY['pending'::character varying, 'verified'::character varying, 'rejected'::character varying]::text[])),
  verified_by uuid,
  verified_at timestamp with time zone,
  created_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT stray_animal_reports_pkey PRIMARY KEY (id),
  CONSTRAINT stray_animal_reports_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id),
  CONSTRAINT stray_animal_reports_animal_type_id_fkey FOREIGN KEY (animal_type_id) REFERENCES public.animal_types(id),
  CONSTRAINT stray_animal_reports_sex_id_fkey FOREIGN KEY (sex_id) REFERENCES public.sexes(id),
  CONSTRAINT stray_animal_reports_collar_status_id_fkey FOREIGN KEY (collar_status_id) REFERENCES public.collar_statuses(id),
  CONSTRAINT stray_animal_reports_color_pattern_id_fkey FOREIGN KEY (color_pattern_id) REFERENCES public.color_patterns(id),
  CONSTRAINT stray_animal_reports_primary_color_id_fkey FOREIGN KEY (primary_color_id) REFERENCES public.primary_colors(id),
  CONSTRAINT stray_animal_reports_verified_by_fkey FOREIGN KEY (verified_by) REFERENCES public.users(id)
);
CREATE TABLE public.user_actions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  report_id uuid,
  admin_id uuid NOT NULL,
  action_type character varying NOT NULL CHECK (action_type::text = ANY (ARRAY['warn'::character varying, 'suspend'::character varying]::text[])),
  reason text NOT NULL,
  created_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT user_actions_pkey PRIMARY KEY (id),
  CONSTRAINT user_actions_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id),
  CONSTRAINT user_actions_admin_id_fkey FOREIGN KEY (admin_id) REFERENCES public.users(id)
);
CREATE TABLE public.users (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  email character varying NOT NULL UNIQUE,
  name character varying NOT NULL,
  role character varying NOT NULL DEFAULT 'user'::character varying CHECK (role::text = ANY (ARRAY['user'::character varying, 'admin'::character varying]::text[])),
  reports_submitted integer NOT NULL DEFAULT 0,
  warnings integer NOT NULL DEFAULT 0,
  status character varying NOT NULL DEFAULT 'active'::character varying CHECK (status::text = ANY (ARRAY['active'::character varying, 'warned'::character varying, 'suspended'::character varying]::text[])),
  created_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT users_pkey PRIMARY KEY (id)
);